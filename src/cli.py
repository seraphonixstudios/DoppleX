# Simple CLI bootstrap (optional, not used by the GUI). Keeps compatibility with non-GUI runs if needed.
import sys
def main():
    print("You2.0 CLI scaffold (Part 1) - not required for GUI operation.")
    sys.exit(0)

if __name__ == '__main__':
    main()
